# AGI orientation notes

See root [`AGI.md`](../AGI.md).

CLI:

```bash
python -m interfaces.cli.main_cli cycle "Daş mövcuddurmu?"
python -m interfaces.cli.main_cli cycle "plan qur" --goal öyrən --json
```
