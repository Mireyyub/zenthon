[package]
name = "zenthon-native-core"
version = "0.1.0"
edition = "2021"
description = "Optional deterministic native helpers for Zenthon"
license = "MIT"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
unicode-normalization = "0.1"
